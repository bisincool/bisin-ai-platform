# 開発日誌 — 2026年3月27日

**プロジェクト**: 合同会社BISIN 公式ウェブサイト
**リポジトリ**: bisincool/bisin-website
**ブランチ**: main
**作業者**: Claude Code (claude-sonnet-4-6)

---

## 本日のコミット一覧

| 時刻 (JST) | ハッシュ | 内容 |
|---|---|---|
| 18:40 | `2d577e6` | 製品・コンテンツ全面リニューアル |
| 18:58 | `194dc70` | UI リデザイン：CoolSpa ブルー医療美容テーマ |
| 19:19 | `d0a9567` | カード：淡金色ガラス・雪花サイズ拡大+融解エフェクト |
| 19:25 | `0b6b1b3` | 雪花：六角結晶・全ページ対応・4枚に変更 |

---

## 作業詳細

### ① 製品・コンテンツ全面リニューアル (`2d577e6`)

**変更ファイル**: `product.html` / `contact.html` / `about.html` / `service.html` / `index.html` / `css/style.css` / `js/i18n.js`
**追加アセット**: `assets/machine.jpg`, `cryo-01-whatis.jpg`, `cryo-02-mechanism.jpg`, `cryo-03-vsdiet.jpg`

#### コンテンツ変更
- **FDA 認証記述を全ページから削除**（法的リスク回避）
  - product.html のバッジ 4 種 → `360°冷却テクノロジー / 非侵襲ボディメイク / 脂肪細胞アプローチ` 3 種に変更
  - i18n.js の全言語（JP/ZH/EN）から FDA 関連キーを除去
- **製品ページ全面刷新**
  - 技術仕様セクション（`product-specs`）を削除
  - 機器写真（`machine.jpg`）を掲載
  - 「5 つの特長」を廃止し「360°脂肪冷却」教育セクション 3 本に置換：
    - Section ① `cryo-section--odd`：360°脂肪冷却とは（`cryo-01-whatis.jpg`）
    - Section ② `cryo-section--even`：脂肪減少の仕組み（`cryo-02-mechanism.jpg`）
    - Section ③ `cryo-section--odd`：通常ダイエットとの違い（`cryo-03-vsdiet.jpg`）
- **お問い合わせページ**: フォームセクションを完全削除し、右カラムに LINE 連絡カードを配置
- **サービスページ**: 全フローステップから「所要時間：xx 分」を削除
- **会社概要ページ**: 価値カード 01 を「最先端 360°脂肪冷却テクノロジーの採用」に更新
- **ナビ CTA**: `無料相談` → `導入相談/無料デモ予約`（全言語）

#### スタイル追加 (`css/style.css`)
- `.cryo-section` / `--odd` / `--even`（交互レイアウト、`direction:rtl` トリック）
- `.cryo-benefits` / `.cryo-benefits__item`（チェックマークリスト）
- `.cryo-compare-cards` / `.cryo-compare-card`（比較カード）
- `.contact-line-wrap` / `.contact-line-card`（LINE 連絡カード）
- `.product-machine-img`（機器写真 + drop-shadow）

---

### ② UI リデザイン — CoolSpa ブルー医療美容テーマ (`194dc70`)

**変更ファイル**: `css/style.css` (+354 行) / `js/particles.js` / 全 HTML ×5

#### カラーシステム全面刷新（`:root` 変数）

| 変数 | 旧値（黒×金） | 新値（ブルー） |
|---|---|---|
| `--color-bg` | `#080807` | `#0A2A52` |
| `--color-bg-2` | `#0d0c09` | `#061C36` |
| `--color-primary` | `#C9A34B`（金） | `#4BB3FF`（アイスブルー） |
| `--color-accent` | `#E8D9B5` | `#EAF6FF` |
| `--color-text` | `#F2EDE4` | `#EAF6FF` |
| `--color-text-muted` | `#9A8E7A` | `#CBD8E7` |
| `--shadow-btn` | `rgba(201,163,75,0.45)` | `rgba(75,179,255,0.55)` |
| `--gradient-hero` | 黒系グラデーション | `#1E4F8A → #123E73 → #0A2A52 → #061C36` |

#### テーマオーバーライドセクション追加（約 200 行）
CSS ファイル末尾に `THEME OVERRIDES` ブロックを追記し、既存コードを破壊せずに全コンポーネントをブルーパレットに更新：
- **ボタン**: `linear-gradient(135deg, #4BB3FF, #1E6EDB)` + ブルーグロー
- **ナビ**: `rgba(6,28,54,0.88)` ダークネイビー、ブルーボーダー
- **ランゲージピッカー**: ゴールド参照をすべてブルーに
- **カード（全種）**: `rgba(255,255,255,0.88)` + `backdrop-filter:blur(8px)` glassmorphism
- **ヒーロー**: ブルーグロー text-shadow
- **cryo セクション**: 半透明ネイビー背景
- **フッター / ラインバナー / CTA**: ブルーボーダー・背景に統一

#### パーティクル変更
- 色: ゴールド `[201,163,75]` → 白 `[255,255,255]`
- 動き: 双方向漂流 → 上から下への降雪（vy 0.30–0.75）
- 数: 80 → 40

---

### ③ カード：淡金色ガラス・雪花改良 (`d0a9567`)

**変更ファイル**: `css/style.css` / `js/particles.js`

#### カード配色変更（白 → 淡金色）

| 要素 | 変更前 | 変更後 |
|---|---|---|
| カード背景 | `rgba(255,255,255,0.88)` | `rgba(255,246,210,0.90)` シャンパンゴールド |
| カードボーダー | `rgba(255,255,255,0.45)` | `rgba(201,163,75,0.28)` ゴールド |
| ホバーボーダー | `rgba(75,179,255,0.35)` | `rgba(201,163,75,0.50)` |
| タイトル文字 | `#1A3050` | `#3A2600` 深琥珀色 |
| 本文文字 | `#4A6A8A` | `#6B4E1A` 暖かみのある茶色 |
| 番号・eyebrow | `var(--color-primary)` | `#8B6514` ゴールドブラウン |
| リスト記号 | ブルー | `#C9A34B` ゴールド |

#### 雪花改良
- サイズ拡大: 半径 0.5–1.5px → **5–8px**（本文フォントサイズと同等）
- 描画: 単純円 → 径方向グラデーション（ソフトなフェード付き円）
- **ヒーローテキストへの衝突融解エフェクト**追加:
  - 対象: `.hero__eyebrow` / `.hero__title` / `.hero__desc` / `.hero__actions` / `.hero__scroll-indicator`
  - ヒット検出: `getBoundingClientRect()` で canvas 座標に変換
  - 融解速度: `meltRate: 0.018`（alpha/frame）、0 になったら上から再生成

---

### ④ 雪花：六角結晶・全ページ対応 (`0b6b1b3`)

**変更ファイル**: `js/particles.js` / 全 HTML ×5（index で旧 canvas 削除）

#### 六角結晶スノーフレーク実装
```
各 arm (×6, 60° 間隔):
  ├─ メイン臂: center → tip (長さ r)
  ├─ 分枝 1: 65% 位置, 60° 結晶角, 長さ 0.30r
  └─ 分枝 2: 38% 位置, 60° 結晶角, 長さ 0.19r
中心ドット: 半径 0.09r
```

#### パラメータ変更

| 設定 | 変更前 | 変更後 |
|---|---|---|
| 数 | 40 | **4** |
| 半径 | 5–8px | **10–16px**（倍サイズ） |
| 速度 Y | 0.30–0.75 | **0.28–0.55**（よりゆっくり） |
| 自転 | なし | **0.0008–0.003 rad/frame**（ランダム方向） |
| 描画 | 径方向グラデーション円 | **6 臂結晶線画** |

#### 全ページ対応
- `particles.js` が `position:fixed; z-index:200` の canvas を自動生成（body に append）
- `index.html`: 旧 `#particles-canvas` 要素を削除
- `about.html` / `product.html` / `service.html` / `contact.html`: `particles.js?v=5` を追加

---

## ファイル変更サマリー（本日合計）

| ファイル | 変更種別 |
|---|---|
| `css/style.css` | 大規模更新（+約 1,000 行追加、旧 gold 変数・スタイル置換） |
| `js/particles.js` | 全面書き直し（3 回: 白雪 → 大粒融解 → 六角結晶） |
| `js/i18n.js` | FDA 削除 + cryo・nav CTA キー追加 |
| `index.html` | CTA テキスト・製品カード・旧 canvas 削除 |
| `product.html` | 技術仕様削除・機器写真・cryo 3 セクション |
| `contact.html` | フォーム削除・LINE カード挿入 |
| `about.html` | 価値カード 01 更新 |
| `service.html` | 所要時間テキスト削除 |
| `assets/machine.jpg` | 新規追加 |
| `assets/cryo-01-whatis.jpg` | 新規追加 |
| `assets/cryo-02-mechanism.jpg` | 新規追加 |
| `assets/cryo-03-vsdiet.jpg` | 新規追加 |

---

## 残タスク（次回対応）

- [ ] cryo セクション ④⑤ — 画像未入稿（ユーザー提供待ち）
- [ ] プライバシーポリシー / 特定商取引法ページ（リンクが `#` のまま）
- [ ] モバイル表示最終確認（金色カード + 六角雪花）
- [ ] Cloudflare Pages デプロイ確認

---

*Generated by Claude Code — 2026-03-27*
